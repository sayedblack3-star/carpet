# Maestro Workflow Context
Generated: 2026-04-12

## Models & Providers
- Primary working model: `GPT-5.4`
- Primary coding environment: `Codex`
- Workflow style: direct in-agent editing, terminal commands, build/verification, and selective git publishing
- Context window size: not explicitly known by the user, so workflow should assume context is valuable and avoid dumping large files unnecessarily

## Workflow Architecture
- Project: `Carpet Land ERP`
- Platforms:
  - Web app
  - Android app via `Capacitor`
  - Windows desktop app via `Electron`
- Core stack:
  - `React`
  - `TypeScript`
  - `Vite`
  - `Tailwind CSS`
  - `Supabase` for auth and database
  - `PostgreSQL`
- Deployment/runtime:
  - Web deployed on `Vercel`
  - Backend logic split between Supabase policies/data and Vercel API routes
- Typical workflow:
  - inspect issue or feature request
  - read relevant files
  - patch code directly
  - run `lint` and `build`
  - optionally run Android/Desktop packaging when needed
  - selectively `commit`/`push` only the intended files

## Tools & Capabilities
- Code editing inside the local workspace
- Terminal-driven verification
- Git operations
- Supabase schema and SQL updates
- Vercel deployment follow-up
- Android release packaging
- Electron desktop packaging

## Quality & Evaluation
- Main verification habits:
  - `npm run lint`
  - `npm run build`
  - targeted manual QA on web, desktop, and mobile flows
- Common workflow pattern:
  - fix issue
  - verify locally
  - publish only the intended surface
- Known quality priorities:
  - avoid regressions on live web
  - keep mobile/desktop changes isolated when needed
  - maintain stable operational flows such as login, seller, cashier, users, and shifts

## Common Failure Modes
- shared-code changes accidentally affecting multiple platforms
- Supabase auth/session edge cases
- RLS or migration drift across SQL files
- UI regressions on desktop vs. mobile layouts
- deployment succeeding while stale cached bundles remain in browser
- packaging/environment issues for Android or Electron

## Constraints
- The user prefers practical execution over abstract planning
- The repository can be dirty, so unrelated changes must not be staged or reverted
- Security matters because the system handles admin, seller, cashier, branches, and operational data
- The workflow should prefer surgical pushes instead of broad rollouts
- Cost/latency are secondary to correctness and stability in coding sessions

## Priorities
1. Quality
2. Stability
3. Safety
4. Speed
5. Cost

## Current Focus
- Improve and maintain `Carpet Land ERP`
- Keep web live and polished
- Ship mobile and desktop updates carefully
- Strengthen structure, security, and maintainability without breaking business-critical flows
